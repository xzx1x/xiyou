# 校园心理咨询记录系统目录说明

本仓库按照“前端 / 后端 / 合约 / 基础设施 / 文档 / 脚本”六大模块拆分，便于后续在非区块链版本基础上迭代隐私保护与链上存证能力。

## 顶层结构
- `packages/`：Bun workspace 管理的多包目录
  - `web-portal/`：Next.js + TypeScript 前端，承载普通用户、心理师、管理员三端页面
  - `server-api/`：Koa + TypeScript 后端，负责接口、权限、数据库访问与后续链上对接
  - `chain-contracts/`：Solidity + Hardhat 合约目录，用于咨询记录上链
- `infra/`：本地与部署环境配置
- `docs/`：需求、架构、测试计划、论文资料等文档
- `scripts/`：脚本与自动化工具
- `tests/`：测试用例

## 运行端口规划
- Web Portal（Next.js）：<http://localhost:3000>
- Server API（Koa）：<http://localhost:3001>
- MySQL：`localhost:3306`

## 本地运行教程
1. **准备环境**  
在命令行输入 `mysql --version`、`node --version`、`bun --version` 检查环境。

2. **修改后端配置**  
需要修改 `packages/server-api/.env`，把 MySQL 密码改成你自己的，例如：

```env
DATABASE_URL=mysql://root:123456@localhost:3306/campus_chain
JWT_SECRET=自己设置一个字符串
PORT=3001
CORS_ORIGIN=http://localhost:3000
```

3. **启动后端**

```powershell
cd packages/server-api
bun install
bun run dev
```

4. **启动前端**

```powershell
cd packages/web-portal
bun install
bun run dev
```
5. 开启私链
cd packages/chain-contracts
bun install
bunx hardhat node

6. 安装并部署合约

```powershell
cd packages/chain-contracts
bun run compile
bun run deploy:consultation
bun run deploy:assessment
```

7. 把部署出来的合约地址填进 `packages/server-api/.env`

```env
CHAIN_RPC_URL=http://127.0.0.1:8545
CHAIN_PRIVATE_KEY=0x你的私钥
CHAIN_ID=31337
CONSULTATION_EVIDENCE_CONTRACT_ADDRESS=0x部署出来的地址
ASSESSMENT_EVIDENCE_CONTRACT_ADDRESS=0x部署出来的地址
```

第二次启动时，只需要分别执行：

```powershell
cd packages/chain-contracts
bunx hardhat node
```

```powershell
cd packages/chain-contracts
bun run compile
bun run deploy:consultation
bun run deploy:assessment
```

```env
CHAIN_RPC_URL=http://127.0.0.1:8545
CHAIN_PRIVATE_KEY=0x你的私钥
CHAIN_ID=31337
CONSULTATION_EVIDENCE_CONTRACT_ADDRESS=0x部署出来的地址
ASSESSMENT_EVIDENCE_CONTRACT_ADDRESS=0x部署出来的地址
```

```powershell
cd packages/server-api
bun run dev
```

```powershell
cd packages/web-portal
bun run dev
``` 

前后端分别监听 `3000/3001` 端口，可直接访问 <http://localhost:3000>。

## 区块链功能操作

如果要启用“咨询记录上链”，只额外做下面这些：





4. 重启后端

```powershell
cd packages/server-api
bun run dev
```

这样心理师创建或更新咨询记录、用户提交心理测评时，都会自动尝试上链。

## 身份识别规则
- 注册时需要提供学号/工号（`identity_code`），系统会写入 `users.identity_code` 并保持唯一
- 合法编号由 `identity_whitelist` 表维护
- 心理师身份不能直接注册，需要先以普通用户身份登录后在站内提交申请，由管理员审核通过后方可切换
