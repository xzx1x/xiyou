# AI 修改项目要求

1. **必须为所有代码添加详尽注释**：尤其是函数与关键变量，需说明其用途、输入/输出或代表的业务含义。其他位置可视实际复杂度酌情注释，但函数和变量是硬性要求，便于后续 AI 继续维护。
2. **角色限制**：系统仅允许注册为普通用户或管理员，心理师（Counselor）身份必须由普通用户登录后在站内提出申请，再由管理员审批切换，任何注册、脚本或接口都不得直接赋予心理师角色。
